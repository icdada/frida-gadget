# Docstrings across this project follow the numpy style: a summary on the second
# line, then Parameters / Returns / Raises sections with dashed underlines.
# This is what Codacy checks through prospector, so declaring it keeps a local
# pydocstyle run in agreement with CI.
[pydocstyle]
convention = numpy
match = .*\.py
